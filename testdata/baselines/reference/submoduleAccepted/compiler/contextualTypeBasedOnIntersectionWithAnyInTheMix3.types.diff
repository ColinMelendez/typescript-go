--- old.contextualTypeBasedOnIntersectionWithAnyInTheMix3.types
+++ new.contextualTypeBasedOnIntersectionWithAnyInTheMix3.types
@@= skipped -21, +21 lines =@@
 >arg : T

 const result = test1({
->result : { num: (1 | 2)[]; str: ("a" | "b")[]; bool: boolean[]; }
->test1({  num: [1, 2],  str: ["a", "b"],  bool: [true, false],}) : { num: (1 | 2)[]; str: ("a" | "b")[]; bool: boolean[]; }
+>result : { num: Array<1 | 2>; str: Array<"a" | "b">; bool: Array<boolean>; }
+>test1({  num: [1, 2],  str: ["a", "b"],  bool: [true, false],}) : { num: Array<1 | 2>; str: Array<"a" | "b">; bool: Array<boolean>; }
 >test1 : <T extends { [K in keyof TypeMap]: TypeMap[K][]; } & { [k: string]: any[]; }>(arg: T) => T
->{  num: [1, 2],  str: ["a", "b"],  bool: [true, false],} : { num: (1 | 2)[]; str: ("a" | "b")[]; bool: boolean[]; }
+>{  num: [1, 2],  str: ["a", "b"],  bool: [true, false],} : { num: Array<1 | 2>; str: Array<"a" | "b">; bool: Array<boolean>; }

   num: [1, 2],
->num : (1 | 2)[]
->[1, 2] : (1 | 2)[]
+>num : Array<1 | 2>
+>[1, 2] : Array<1 | 2>
 >1 : 1
 >2 : 2

   str: ["a", "b"],
->str : ("a" | "b")[]
->["a", "b"] : ("a" | "b")[]
+>str : Array<"a" | "b">
+>["a", "b"] : Array<"a" | "b">
 >"a" : "a"
 >"b" : "b"

   bool: [true, false],
->bool : boolean[]
->[true, false] : boolean[]
+>bool : Array<boolean>
+>[true, false] : Array<boolean>
 >true : true
 >false : false

@@= skipped -41, +41 lines =@@
 // https://github.com/microsoft/TypeScript/issues/59473

 const x: { ml: any } & { ml: 'edge' } = { ml: 'edge' };
->x : { ml: any; } & { ml: "edge"; }
+>x : { ml: any; } & { ml: 'edge'; }
 >ml : any
 >ml : "edge"
 >{ ml: 'edge' } : { ml: "edge"; }
@@= skipped -13, +13 lines =@@
 >1 : 1

 const b: any[] & 1[] = [1, 1];
->b : any[] & 1[]
->[1, 1] : 1[]
+>b : Array<any> & Array<1>
+>[1, 1] : Array<1>
 >1 : 1
 >1 : 1

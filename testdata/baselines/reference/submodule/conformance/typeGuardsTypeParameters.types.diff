--- old.typeGuardsTypeParameters.types
+++ new.typeGuardsTypeParameters.types
@@= skipped -66, +66 lines =@@
 >item : { [P in keyof T]: T[P]; }

     const strings: string[] = [];
->strings : string[]
->[] : never[]
+>strings : Array<string>
+>[] : Array<never>

     for (const key in item) {
 >key : Extract<keyof T, string>
@@= skipped -21, +21 lines =@@

             strings.push(value);
 >strings.push(value) : number
->strings.push : (...items: string[]) => number
->strings : string[]
->push : (...items: string[]) => number
+>strings.push : (...items: Array<string>) => number
+>strings : Array<string>
+>push : (...items: Array<string>) => number
 >value : { [P in keyof T]: T[P]; }[Extract<keyof T, string>] & string
         }
     }
//// [tests/cases/conformance/statements/for-ofStatements/ES5For-of6.ts] ////

=== ES5For-of6.ts ===
for (var w of []) {
>w : any
>[] : Array<undefined>

    for (var v of []) {
>v : any
>[] : Array<undefined>

        var x = [w, v];
>x : Array<any>
>[w, v] : Array<any>
>w : any
>v : any
    }
}

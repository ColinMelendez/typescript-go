--- old.assertionTypePredicates1.types
+++ new.assertionTypePredicates1.types
@@= skipped -103, +103 lines =@@
         x[0].length;
 >x[0].length : number
 >x[0] : string
->x : string[]
+>x : Array<string>
 >0 : 0
 >length : number
     }
@@= skipped -17, +17 lines =@@
         x[0].length;
 >x[0].length : number
 >x[0] : string
->x : string[]
+>x : Array<string>
 >0 : 0
 >length : number
     }
@@= skipped -398, +398 lines =@@

 function f11(items: Test[]) {
 >f11 : (items: Test[]) => void
->items : Test[]
+>items : Array<Test>

     for (let item of items) {
 >item : Test
->items : Test[]
+>items : Array<Test>

         if (item.isTest2()) {
 >item.isTest2() : boolean
@@= skipped -80, +80 lines =@@
 >"string" : "string"

     const a = [assert];
->a : ((value: unknown) => asserts value)[]
->[assert] : ((value: unknown) => asserts value)[]
+>a : Array<(value: unknown) => asserts value>
+>[assert] : Array<(value: unknown) => asserts value>
 >assert : (value: unknown) => asserts value

     a[0](typeof x === "string");  // Error
 >a[0](typeof x === "string") : void
 >a[0] : (value: unknown) => asserts value
->a : ((value: unknown) => asserts value)[]
+>a : Array<(value: unknown) => asserts value>
 >0 : 0
 >typeof x === "string" : boolean
 >typeof x : "bigint" | "boolean" | "function" | "number" | "object" | "string" | "symbol" | "undefined"
@@= skipped -63, +63 lines =@@

 function example1(things: Thing[]) {
 >example1 : (things: Thing[]) => void
->things : Thing[]
+>things : Array<Thing>

     for (let thing of things) {
 >thing : Thing
->things : Thing[]
+>things : Array<Thing>

         thing.isGood();
 >thing.isGood() : void
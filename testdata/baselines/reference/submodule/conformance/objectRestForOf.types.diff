--- old.objectRestForOf.types
+++ new.objectRestForOf.types
@@= skipped -1, +1 lines =@@

 === objectRestForOf.ts ===
 let array: { x: number, y: string }[];
->array : { x: number; y: string; }[]
+>array : Array<{ x: number; y: string; }>
 >x : number
 >y : string

 for (let { x, ...restOf } of array) {
 >x : number
 >restOf : { y: string; }
->array : { x: number; y: string; }[]
+>array : Array<{ x: number; y: string; }>

     [x, restOf];
->[x, restOf] : (number | { y: string; })[]
+>[x, restOf] : Array<number | { y: string; }>
 >x : number
 >restOf : { y: string; }
 }
@@= skipped -26, +26 lines =@@
 >x : number
 >xx : number
 >rrestOff : { y: string; }
->array : { x: number; y: string; }[]
+>array : Array<{ x: number; y: string; }>

     [xx, rrestOff];
->[xx, rrestOff] : (number | { y: string; })[]
+>[xx, rrestOff] : Array<number | { y: string; }>
 >xx : number
 >rrestOff : { y: string; }
 }
 for (const norest of array.map(a => ({ ...a, x: 'a string' }))) {
 >norest : { y: string; x: string; }
->array.map(a => ({ ...a, x: 'a string' })) : { y: string; x: string; }[]
->array.map : <U>(callbackfn: (value: { x: number; y: string; }, index: number, array: { x: number; y: string; }[]) => U, thisArg?: any) => U[]
->array : { x: number; y: string; }[]
->map : <U>(callbackfn: (value: { x: number; y: string; }, index: number, array: { x: number; y: string; }[]) => U, thisArg?: any) => U[]
+>array.map(a => ({ ...a, x: 'a string' })) : Array<{ y: string; x: string; }>
+>array.map : <U>(callbackfn: (value: { x: number; y: string; }, index: number, array: Array<{ x: number; y: string; }>) => U, thisArg?: any) => Array<U>
+>array : Array<{ x: number; y: string; }>
+>map : <U>(callbackfn: (value: { x: number; y: string; }, index: number, array: Array<{ x: number; y: string; }>) => U, thisArg?: any) => Array<U>
 >a => ({ ...a, x: 'a string' }) : (a: { x: number; y: string; }) => { y: string; x: string; }
 >a : { x: number; y: string; }
 >({ ...a, x: 'a string' }) : { y: string; x: string; }
@@= skipped -22, +22 lines =@@
 >'a string' : "a string"

     [norest.x, norest.y];
->[norest.x, norest.y] : string[]
+>[norest.x, norest.y] : Array<string>
 >norest.x : string
 >norest : { y: string; x: string; }
 >x : string
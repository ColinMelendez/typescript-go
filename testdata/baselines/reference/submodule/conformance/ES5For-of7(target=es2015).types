//// [tests/cases/conformance/statements/for-ofStatements/ES5For-of7.ts] ////

=== ES5For-of7.ts ===
for (var w of []) {
>w : never
>[] : Array<never>

    var x = w;
>x : never
>w : never
}

for (var v of []) {
>v : never
>[] : Array<never>

    var x = [w, v];
>x : never
>[w, v] : Array<never>
>w : never
>v : never
}

--- old.importTag25.types
+++ new.importTag25.types
@@= skipped -12, +12 lines =@@
 /** @import { T } from "./types.d.ts" */

 export default async function f() {
->f : () => Promise<T[]>
+>f : () => Promise<Array<T>>

 	/** @type {T[]} */
 	const types = [];
->types : T[]
->[] : never[]
+>types : Array<T>
+>[] : Array<never>

 	return types;
->types : T[]
+>types : Array<T>
 }

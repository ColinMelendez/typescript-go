--- old.ES5For-of16(target=es2015).types
+++ new.ES5For-of16(target=es2015).types
@@= skipped -2, +2 lines =@@
 === ES5For-of16.ts ===
 for (let v of []) {
 >v : any
->[] : undefined[]
+>[] : Array<undefined>

     v;
 >v : any

     for (let v of []) {
 >v : any
->[] : undefined[]
+>[] : Array<undefined>

         var x = v;
 >x : any
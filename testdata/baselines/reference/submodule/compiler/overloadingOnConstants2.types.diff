--- old.overloadingOnConstants2.types
+++ new.overloadingOnConstants2.types
@@= skipped -21, +21 lines =@@
 function foo(x: "hi", items: string[]): D;
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >x : "hi"
->items : string[]
+>items : Array<string>

 function foo(x: "bye", items: string[]): E;
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >x : "bye"
->items : string[]
+>items : Array<string>

 function foo(x: string, items: string[]): C {
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >x : string
->items : string[]
+>items : Array<string>

    return null;
 }
@@= skipped -19, +19 lines =@@
 >foo("hi", []) : D
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >"hi" : "hi"
->[] : never[]
+>[] : Array<never>

 var b: E = foo("bye", []); // E 
 >b : E
 >foo("bye", []) : E
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >"bye" : "bye"
->[] : never[]
+>[] : Array<never>

 var c = foo("um", []); // error
 >c : D & E
 >foo("um", []) : D & E
 >foo : { (x: "hi", items: string[]): D; (x: "bye", items: string[]): E; }
 >"um" : "um"
->[] : never[]
+>[] : Array<never>


 //function bar(x: "hi", items: string[]): D;
 function bar(x: "bye", items: string[]): E;
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >x : "bye"
->items : string[]
+>items : Array<string>

 function bar(x: string, items: string[]): C;
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >x : string
->items : string[]
+>items : Array<string>

 function bar(x: string, items: string[]): C {
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >x : string
->items : string[]
+>items : Array<string>

    return null;
 }
@@= skipped -41, +41 lines =@@
 >bar("hi", []) : C
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >"hi" : "hi"
->[] : never[]
+>[] : Array<never>

 var e: E = bar("bye", []); // E 
 >e : E
 >bar("bye", []) : E
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >"bye" : "bye"
->[] : never[]
+>[] : Array<never>

 var f: C = bar("um", []); // C
 >f : C
 >bar("um", []) : C
 >bar : { (x: "bye", items: string[]): E; (x: string, items: string[]): C; }
 >"um" : "um"
->[] : never[]
+>[] : Array<never>

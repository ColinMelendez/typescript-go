//// [tests/cases/compiler/genericMethodOverspecialization.ts] ////

=== genericMethodOverspecialization.ts ===
var names = ["list", "table1", "table2", "table3", "summary"];
>names : Array<string>
>["list", "table1", "table2", "table3", "summary"] : Array<string>
>"list" : "list"
>"table1" : "table1"
>"table2" : "table2"
>"table3" : "table3"
>"summary" : "summary"

interface HTMLElement {
    clientWidth: number;
>clientWidth : number

    isDisabled: boolean;
>isDisabled : boolean
}

declare var document: Document;
>document : Document

interface Document {
    getElementById(elementId: string): HTMLElement;
>getElementById : { (elementId: string): HTMLElement | null; (elementId: string): HTMLElement; }
>elementId : string
}

var elements = names.map(function (name) {
>elements : Array<HTMLElement>
>names.map(function (name) {    return document.getElementById(name);}) : Array<HTMLElement>
>names.map : <U>(callbackfn: (value: string, index: number, array: Array<string>) => U, thisArg?: any) => Array<U>
>names : Array<string>
>map : <U>(callbackfn: (value: string, index: number, array: Array<string>) => U, thisArg?: any) => Array<U>
>function (name) {    return document.getElementById(name);} : (name: string) => HTMLElement
>name : string

    return document.getElementById(name);
>document.getElementById(name) : HTMLElement
>document.getElementById : { (elementId: string): HTMLElement | null; (elementId: string): HTMLElement; }
>document : Document
>getElementById : { (elementId: string): HTMLElement | null; (elementId: string): HTMLElement; }
>name : string

});


var xxx = elements.filter(function (e) {
>xxx : Array<HTMLElement>
>elements.filter(function (e) {    return !e.isDisabled;}) : Array<HTMLElement>
>elements.filter : { <S extends HTMLElement>(predicate: (value: HTMLElement, index: number, array: Array<HTMLElement>) => value is S, thisArg?: any): Array<S>; (predicate: (value: HTMLElement, index: number, array: Array<HTMLElement>) => unknown, thisArg?: any): Array<HTMLElement>; }
>elements : Array<HTMLElement>
>filter : { <S extends HTMLElement>(predicate: (value: HTMLElement, index: number, array: Array<HTMLElement>) => value is S, thisArg?: any): Array<S>; (predicate: (value: HTMLElement, index: number, array: Array<HTMLElement>) => unknown, thisArg?: any): Array<HTMLElement>; }
>function (e) {    return !e.isDisabled;} : (e: HTMLElement) => boolean
>e : HTMLElement

    return !e.isDisabled;
>!e.isDisabled : boolean
>e.isDisabled : boolean
>e : HTMLElement
>isDisabled : boolean

});

var widths:number[] = elements.map(function (e) { // should not error
>widths : Array<number>
>elements.map(function (e) { // should not error    return e.clientWidth;}) : Array<number>
>elements.map : <U>(callbackfn: (value: HTMLElement, index: number, array: Array<HTMLElement>) => U, thisArg?: any) => Array<U>
>elements : Array<HTMLElement>
>map : <U>(callbackfn: (value: HTMLElement, index: number, array: Array<HTMLElement>) => U, thisArg?: any) => Array<U>
>function (e) { // should not error    return e.clientWidth;} : (e: HTMLElement) => number
>e : HTMLElement

    return e.clientWidth;
>e.clientWidth : number
>e : HTMLElement
>clientWidth : number

});



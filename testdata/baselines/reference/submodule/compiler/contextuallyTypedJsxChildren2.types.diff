--- old.contextuallyTypedJsxChildren2.types
+++ new.contextuallyTypedJsxChildren2.types
@@= skipped -153, +153 lines =@@
 >Subscribe : <TSelected = State>(props: { selector?: (state: State) => TSelected; children: (state: TSelected) => void; }) => React.ReactElement<any>

     selector={(state) => {
->selector : (state: State) => boolean[]
->(state) => {      return [state.value]    } : (state: State) => boolean[]
+>selector : (state: State) => Array<boolean>
+>(state) => {      return [state.value]    } : (state: State) => Array<boolean>
 >state : State

       return [state.value]
->[state.value] : boolean[]
+>[state.value] : Array<boolean>
 >state.value : boolean
 >state : State
 >value : boolean
@@= skipped -13, +13 lines =@@
     }}
   >
     {([value = false]) => {
->([value = false]) => {      console.log(value)    } : ([value]: boolean[]) => void
+>([value = false]) => {      console.log(value)    } : ([value]: Array<boolean>) => void
 >value : boolean
 >false : false

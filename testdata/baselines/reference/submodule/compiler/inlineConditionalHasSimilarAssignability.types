//// [tests/cases/compiler/inlineConditionalHasSimilarAssignability.ts] ////

=== inlineConditionalHasSimilarAssignability.ts ===
type MyExtract<T, U> = T extends U ? T : never
>MyExtract : MyExtract<T, U>

function foo<T>(a: T) {
>foo : <T>(a: T) => void
>a : T

  const b: Extract<any[], T> = 0 as any;
>b : Extract<Array<any>, T>
>0 as any : any
>0 : 0

  a = b; // ok
>a = b : Extract<Array<any>, T>
>a : T
>b : Extract<Array<any>, T>

  const c: (any[] extends T ? any[] : never) = 0 as any;
>c : Array<any> extends T ? Array<any> : never
>0 as any : any
>0 : 0

  a = c;
>a = c : Array<any> extends T ? Array<any> : never
>a : T
>c : Array<any> extends T ? Array<any> : never

  const d: MyExtract<any[], T> = 0 as any;
>d : MyExtract<Array<any>, T>
>0 as any : any
>0 : 0

  a = d; // ok
>a = d : MyExtract<Array<any>, T>
>a : T
>d : MyExtract<Array<any>, T>

  type CustomType = any[] extends T ? any[] : never;
>CustomType : Array<any> extends T ? Array<any> : never

  const e: CustomType = 0 as any;
>e : Array<any> extends T ? Array<any> : never
>0 as any : any
>0 : 0

  a = e;
>a = e : Array<any> extends T ? Array<any> : never
>a : T
>e : Array<any> extends T ? Array<any> : never
}

//// [tests/cases/compiler/collisionArgumentsClassMethod.ts] ////

=== collisionArgumentsClassMethod.ts ===
class c1 {
>c1 : c1

    public foo(i: number, ...arguments) { //arguments is error
>foo : (i: number, ...arguments: Array<any>) => void
>i : number
>arguments : Array<any>

        var arguments: any[]; // no error
>arguments : Array<any>
    }
    public foo1(arguments: number, ...rest) { //arguments is error
>foo1 : (arguments: number, ...rest: Array<any>) => void
>arguments : number
>rest : Array<any>

        var arguments = 10; // no error
>arguments : number
>10 : 10
    }
    public fooNoError(arguments: number) { // no error
>fooNoError : (arguments: number) => void
>arguments : number

        var arguments = 10; // no error
>arguments : number
>10 : 10
    }
    public f4(i: number, ...arguments); // no codegen no error
>f4 : { (i: number, ...arguments: Array<any>): any; (i: string, ...arguments: Array<any>): any; }
>i : number
>arguments : Array<any>

    public f4(i: string, ...arguments); // no codegen no error
>f4 : { (i: number, ...arguments: Array<any>): any; (i: string, ...arguments: Array<any>): any; }
>i : string
>arguments : Array<any>

    public f4(i: any, ...arguments) { // error
>f4 : { (i: number, ...arguments: Array<any>): any; (i: string, ...arguments: Array<any>): any; }
>i : any
>arguments : Array<any>

        var arguments: any[]; // no error
>arguments : Array<any>
    }
    public f41(arguments: number, ...rest); // no codegen no error
>f41 : { (arguments: number, ...rest: Array<any>): any; (arguments: string, ...rest: Array<any>): any; }
>arguments : number
>rest : Array<any>

    public f41(arguments: string, ...rest); // no codegen no error
>f41 : { (arguments: number, ...rest: Array<any>): any; (arguments: string, ...rest: Array<any>): any; }
>arguments : string
>rest : Array<any>

    public f41(arguments: any, ...rest) { // error
>f41 : { (arguments: number, ...rest: Array<any>): any; (arguments: string, ...rest: Array<any>): any; }
>arguments : any
>rest : Array<any>

        var arguments: any; // no error
>arguments : any
    }
    public f4NoError(arguments: number); // no error
>f4NoError : { (arguments: number): any; (arguments: string): any; }
>arguments : number

    public f4NoError(arguments: string); // no error
>f4NoError : { (arguments: number): any; (arguments: string): any; }
>arguments : string

    public f4NoError(arguments: any) { // no error
>f4NoError : { (arguments: number): any; (arguments: string): any; }
>arguments : any

        var arguments: any; // no error
>arguments : any
    }
}

declare class c2 {
>c2 : c2

    public foo(i: number, ...arguments); // No error - no code gen
>foo : (i: number, ...arguments: Array<any>) => any
>i : number
>arguments : Array<any>

    public foo1(arguments: number, ...rest); // No error - no code gen
>foo1 : (arguments: number, ...rest: Array<any>) => any
>arguments : number
>rest : Array<any>

    public fooNoError(arguments: number); // No error - no code gen
>fooNoError : (arguments: number) => any
>arguments : number

    public f4(i: number, ...arguments); // no codegen no error
>f4 : { (i: number, ...arguments: Array<any>): any; (i: string, ...arguments: Array<any>): any; }
>i : number
>arguments : Array<any>

    public f4(i: string, ...arguments); // no codegen no error
>f4 : { (i: number, ...arguments: Array<any>): any; (i: string, ...arguments: Array<any>): any; }
>i : string
>arguments : Array<any>

    public f41(arguments: number, ...rest); // no codegen no error
>f41 : { (arguments: number, ...rest: Array<any>): any; (arguments: string, ...rest: Array<any>): any; }
>arguments : number
>rest : Array<any>

    public f41(arguments: string, ...rest); // no codegen no error
>f41 : { (arguments: number, ...rest: Array<any>): any; (arguments: string, ...rest: Array<any>): any; }
>arguments : string
>rest : Array<any>

    public f4NoError(arguments: number); // no error
>f4NoError : { (arguments: number): any; (arguments: string): any; }
>arguments : number

    public f4NoError(arguments: string); // no error
>f4NoError : { (arguments: number): any; (arguments: string): any; }
>arguments : string
}

class c3 {
>c3 : c3

    public foo(...restParameters) {
>foo : (...restParameters: Array<any>) => void
>restParameters : Array<any>

        var arguments = 10; // no error
>arguments : number
>10 : 10
    }
    public fooNoError() {
>fooNoError : () => void

        var arguments = 10; // no error
>arguments : number
>10 : 10
    }
}

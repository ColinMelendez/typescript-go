//// [tests/cases/compiler/partialDiscriminatedUnionMemberHasGoodError.ts] ////

=== partialDiscriminatedUnionMemberHasGoodError.ts ===
interface TypeA {
    type: "A";
>type : "A"

    param: string;
>param : string
}

interface TypeB {
    type: "B";
>type : "B"

    param: string;
>param : string
}

type ValidType = TypeA | TypeB;
>ValidType : ValidType

interface Wrapper {
    types: ValidType[];
>types : Array<ValidType>
}

const foo: Wrapper[] = [];
>foo : Array<Wrapper>
>[] : Array<never>

foo.push({
>foo.push({    types: [{        type: "A"    }]}) : number
>foo.push : (...items: Array<Wrapper>) => number
>foo : Array<Wrapper>
>push : (...items: Array<Wrapper>) => number
>{    types: [{        type: "A"    }]} : { types: Array<{ type: "A"; }>; }

    types: [{
>types : Array<{ type: "A"; }>
>[{        type: "A"    }] : Array<{ type: "A"; }>
>{        type: "A"    } : { type: "A"; }

        type: "A"
>type : "A"
>"A" : "A"

    }]
});

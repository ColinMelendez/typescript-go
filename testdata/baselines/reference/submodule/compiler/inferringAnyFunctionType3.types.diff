--- old.inferringAnyFunctionType3.types
+++ new.inferringAnyFunctionType3.types
@@= skipped -10, +10 lines =@@
 }

 var v = f([x => x]);
->v : ((x: number) => number)[]
->f([x => x]) : ((x: number) => number)[]
+>v : Array<(x: number) => number>
+>f([x => x]) : Array<(x: number) => number>
 >f : <T extends ((p1: number) => number)[]>(p: T) => T
->[x => x] : ((x: number) => number)[]
+>[x => x] : Array<(x: number) => number>
 >x => x : (x: number) => number
 >x : number
 >x : number
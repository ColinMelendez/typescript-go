//// [tests/cases/compiler/deeplyNestedCheck.ts] ////

=== deeplyNestedCheck.ts ===
// Repro from #14794

interface DataSnapshot<X = {}> {
  child(path: string): DataSnapshot;
>child : (path: string) => DataSnapshot
>path : string
}

interface Snapshot<T> extends DataSnapshot {
  child<U extends Extract<keyof T, string>>(path: U): Snapshot<T[U]>;
>child : <U extends Extract<keyof T, string>>(path: U) => Snapshot<T[U]>
>path : U
}

// Repro from 34619

interface A { b: B[] }
>b : Array<B>

interface B { c: C }
>c : C

interface C { d: D[] }
>d : Array<D>

interface D { e: E[] }
>e : Array<E>

interface E { f: F[] }
>f : Array<F>

interface F { g: G }
>g : G

interface G { h: H[] }
>h : Array<H>

interface H { i: string }
>i : string

const x: A = {
>x : A
>{  b: [    {      c: {        d: [          {            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          },        ],      },    },  ],} : { b: Array<{ c: { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }; }>; }

  b: [
>b : Array<{ c: { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }; }>
>[    {      c: {        d: [          {            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          },        ],      },    },  ] : Array<{ c: { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }; }>
    {
>{      c: {        d: [          {            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          },        ],      },    } : { c: { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }; }

      c: {
>c : { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }
>{        d: [          {            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          },        ],      } : { d: Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>; }

        d: [
>d : Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>
>[          {            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          },        ] : Array<{ e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }>
          {
>{            e: [              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ],          } : { e: Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>; }

            e: [
>e : Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>
>[              {                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              },            ] : Array<{ f: Array<{ g: { h: Array<{}>; }; }>; }>
              {
>{                f: [                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ],              } : { f: Array<{ g: { h: Array<{}>; }; }>; }

                f: [
>f : Array<{ g: { h: Array<{}>; }; }>
>[                  {                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  },                ] : Array<{ g: { h: Array<{}>; }; }>
                  {
>{                    g: {                      h: [                        {                          // i: '',                        },                      ],                    },                  } : { g: { h: Array<{}>; }; }

                    g: {
>g : { h: Array<{}>; }
>{                      h: [                        {                          // i: '',                        },                      ],                    } : { h: Array<{}>; }

                      h: [
>h : Array<{}>
>[                        {                          // i: '',                        },                      ] : Array<{}>
                        {
>{                          // i: '',                        } : {}

                          // i: '',
                        },
                      ],
                    },
                  },
                ],
              },
            ],
          },
        ],
      },
    },
  ],
};

// Repro from 34619

const a1: string[][][][][] = [[[[[42]]]]];
>a1 : Array<Array<Array<Array<Array<string>>>>>
>[[[[[42]]]]] : Array<Array<Array<Array<Array<number>>>>>
>[[[[42]]]] : Array<Array<Array<Array<number>>>>
>[[[42]]] : Array<Array<Array<number>>>
>[[42]] : Array<Array<number>>
>[42] : Array<number>
>42 : 42

const a2: string[][][][][][][][][][] = [[[[[[[[[[42]]]]]]]]]];
>a2 : Array<Array<Array<Array<Array<Array<Array<Array<Array<Array<string>>>>>>>>>>
>[[[[[[[[[[42]]]]]]]]]] : Array<Array<Array<Array<Array<Array<Array<Array<Array<Array<number>>>>>>>>>>
>[[[[[[[[[42]]]]]]]]] : Array<Array<Array<Array<Array<Array<Array<Array<Array<number>>>>>>>>>
>[[[[[[[[42]]]]]]]] : Array<Array<Array<Array<Array<Array<Array<Array<number>>>>>>>>
>[[[[[[[42]]]]]]] : Array<Array<Array<Array<Array<Array<Array<number>>>>>>>
>[[[[[[42]]]]]] : Array<Array<Array<Array<Array<Array<number>>>>>>
>[[[[[42]]]]] : Array<Array<Array<Array<Array<number>>>>>
>[[[[42]]]] : Array<Array<Array<Array<number>>>>
>[[[42]]] : Array<Array<Array<number>>>
>[[42]] : Array<Array<number>>
>[42] : Array<number>
>42 : 42


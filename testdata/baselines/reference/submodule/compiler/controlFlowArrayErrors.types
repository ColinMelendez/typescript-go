//// [tests/cases/compiler/controlFlowArrayErrors.ts] ////

=== controlFlowArrayErrors.ts ===
declare function cond(): boolean;
>cond : () => boolean

function f1() {
>f1 : () => void

    let x = [];  // Implicit any[] error in some locations
>x : Array<any>
>[] : Array<never>

    let y = x;   // Implicit any[] error
>y : Array<any>
>x : Array<any>

    x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

    let z = x;
>z : Array<number>
>x : Array<number>
}

function f2() {
>f2 : () => void

    let x;       // Implicit any[] error in some locations
>x : any

    x = [];
>x = [] : Array<never>
>x : any
>[] : Array<never>

    let y = x;   // Implicit any[] error
>y : Array<any>
>x : Array<any>

    x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

    let z = x;
>z : Array<number>
>x : Array<number>
}

function f3() {
>f3 : () => void

    let x = [];  // Implicit any[] error in some locations
>x : Array<any>
>[] : Array<never>

    x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

    function g() {
>g : () => void

        x;       // Implicit any[] error
>x : Array<any>
    }
}

function f4() {
>f4 : () => void

    let x;
>x : any

    x = [5, "hello"];  // Non-evolving array
>x = [5, "hello"] : Array<string | number>
>x : any
>[5, "hello"] : Array<string | number>
>5 : 5
>"hello" : "hello"

    x.push(true);      // Error
>x.push(true) : number
>x.push : (...items: Array<string | number>) => number
>x : Array<string | number>
>push : (...items: Array<string | number>) => number
>true : true
}

function f5() {
>f5 : () => void

    let x = [5, "hello"];  // Non-evolving array
>x : Array<string | number>
>[5, "hello"] : Array<string | number>
>5 : 5
>"hello" : "hello"

    x.push(true);          // Error
>x.push(true) : number
>x.push : (...items: Array<string | number>) => number
>x : Array<string | number>
>push : (...items: Array<string | number>) => number
>true : true
}

function f6() {
>f6 : () => void

    let x;
>x : any

    if (cond()) {
>cond() : boolean
>cond : () => boolean

        x = [];
>x = [] : Array<never>
>x : any
>[] : Array<never>

        x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

        x.push("hello");
>x.push("hello") : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>"hello" : "hello"
    }
    else {
        x = [true];  // Non-evolving array
>x = [true] : Array<boolean>
>x : any
>[true] : Array<boolean>
>true : true
    }
    x;           // boolean[] | (string | number)[]
>x : Array<string | number> | Array<boolean>

    x.push(99);  // Error
>x.push(99) : number
>x.push : ((...items: Array<string | number>) => number) | ((...items: Array<boolean>) => number)
>x : Array<string | number> | Array<boolean>
>push : ((...items: Array<string | number>) => number) | ((...items: Array<boolean>) => number)
>99 : 99
}

function f7() {
>f7 : () => void

    let x = [];       // x has evolving array value
>x : Array<any>
>[] : Array<never>

    x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

    let y = x;        // y has non-evolving array value
>y : Array<number>
>x : Array<number>

    x.push("hello");  // Ok
>x.push("hello") : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>"hello" : "hello"

    y.push("hello");  // Error
>y.push("hello") : number
>y.push : (...items: Array<number>) => number
>y : Array<number>
>push : (...items: Array<number>) => number
>"hello" : "hello"
}

function f8() {
>f8 : () => void

    const x = [];  // Implicit any[] error in some locations
>x : Array<any>
>[] : Array<never>

    x.push(5);
>x.push(5) : number
>x.push : (...items: Array<any>) => number
>x : Array<any>
>push : (...items: Array<any>) => number
>5 : 5

    function g() {
>g : () => void

        x;  // Implicit any[] error
>x : Array<any>
    }
}

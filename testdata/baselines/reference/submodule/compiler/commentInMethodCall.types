//// [tests/cases/compiler/commentInMethodCall.ts] ////

=== commentInMethodCall.ts ===
//commment here
var s: string[];
>s : Array<string>

s.map(// do something
>s.map(// do something    function () { }) : Array<void>
>s.map : <U>(callbackfn: (value: string, index: number, array: Array<string>) => U, thisArg?: any) => Array<U>
>s : Array<string>
>map : <U>(callbackfn: (value: string, index: number, array: Array<string>) => U, thisArg?: any) => Array<U>

    function () { });
>function () { } : () => void

